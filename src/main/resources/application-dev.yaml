spring:
  datasource:
    url: jdbc:mysql://localhost:3306/renshi?characterEncoding=utf8&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: false
logging:
  level:
    root: info